The uploaded executor report records three runner starts and two network matrix executions. The delivered archive itself proves transport failure but cannot independently count starts because the implementation had no durable invocation marker. The executor report is therefore retained as operator-supplied evidence, while the package is retained as independently hash-verified machine evidence. Both support rejection of the scope decision; neither supports a GLOBAL availability or unavailability conclusion.
